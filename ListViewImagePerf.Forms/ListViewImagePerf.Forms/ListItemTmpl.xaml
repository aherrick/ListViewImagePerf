<?xml version="1.0" encoding="utf-8" ?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms"
          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
          xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
          x:Class="ListViewImagePerf.Forms.ListItemTmpl">

    <ViewCell.View>

        <Grid Padding="8,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ffimageloading:CachedImage x:Name="img"
                HorizontalOptions="Center" VerticalOptions="Center"
            DownsampleToViewSize="True"
                        Aspect="AspectFill"
        WidthRequest="75" HeightRequest="75" />

            <StackLayout Grid.Column="1"
                   Padding="4"
                   Spacing="2"
                   VerticalOptions="Center">
                <Label Text="{Binding Location}" FontSize="Small" FontAttributes="Bold"   TextColor="#547799" LineBreakMode="TailTruncation" />
            </StackLayout>
        </Grid>
    </ViewCell.View>
</ViewCell>