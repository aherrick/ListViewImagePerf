<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:ListViewImagePerf.Maui"
             x:Class="ListViewImagePerf.Maui.MainPage"
             BackgroundColor="{DynamicResource SecondaryColor}">

    <StackLayout>
        <StackLayout>
            <CollectionView x:Name="lstView">
                <CollectionView.ItemTemplate>
                    <DataTemplate>

                        <views:ListItemTmpl />
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>

        <!--<ListView x:Name="lstView" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Label Text="{Binding Location}"></Label>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->
    </StackLayout>
</ContentPage>